## [2026-04-13 16:44] | 👥 группа | Виктор Степаненко
**Контекст:** Проект: всемирный-кэшбек
Группа: Валеричи на IPO
**Замечание:** а давай постобработку голосовых настроим через онлайн кнопки всё таки, которые будут доступны всем пользователям в группе
**Решение:** —

---

## [2026-04-13 16:33] | 📱 личка | Виктор Степаненко
**Контекст:** Проект: всемирный-кэшбек
Этап: 0_setup
**Замечание:** надо определить механизм который будет чистить твой фидбэк после исправления
**Решение:** —

---

## [2026-04-13 14:57] | 📱 личка | Виктор Степаненко
**Контекст:** Проект: всемирный-кэшбек
Этап: 0_setup
**Замечание:** Господа Валеричи, к сожалению, у меня нет доступа к логам или технической инфраструктуре проекта. Я здесь, чтобы помочь с маркетинговыми и стратегическими вопросами, а не с техническими деталями. Если你需要查看日志，可能需要联系负责技术的同事。😊

（注：最后一句是中文，意思是“如果你需要查看日志，可能需要联系负责技术的同事。”如果你不希望包含中文，可以忽略这一部分。）

If you need to check the logs, it might be best to reach out to a technical team member. 😊


вот такой ответ был
**Решение:** —

---

## [2026-04-13 14:12] | 📱 личка | Виктор Степаненко
**Контекст:** Проект: всемирный-кэшбек
Этап: 0_setup
**Замечание:** в персональных переписках персональное обращение, не может быть что ты ко мне обращаешься Валеричи
**Решение:** —

---

## [2026-04-13 12:02] | 👥 группа | Виктор Степаненко
**Контекст:** Проект: всемирный-кэшбек
Группа: Валеричи на IPO
**Замечание:** А ты же можешь делать постобработку голосовых, правильно?
**Решение:** —

---

## [2026-04-13 11:04] | 👥 группа | Виктор Степаненко
**Контекст:** Проект: всемирный-кэшбек
Группа: Валеричи на IPO
**Замечание:** А ты чего вдруг решил поздороваться, когда вы уже всё утро общаетесь?
**Решение:** —

---

## [2026-04-13 08:24] | 👥 группа | Sergey Sednev
**Контекст:** Проект: всемирный-кэшбек
Группа: Валеричи на IPO
**Замечание:** спорно, практически ничего нового, риски не учтены
**Решение:** —

---

## [2026-04-10 23:02] | 👥 группа | Виктор Степаненко
**Контекст:** Проект: всемирный-кэшбек
Группа: Валеричи на IPO
**Замечание:** заглючил и написал на китайском
**Решение:** —

---

# Журнал улучшений бота

Источники: 📱 личка | 👥 группа | 🌐 webui | 💬 claude-session

---

## [2026-04-13] | 💬 claude-session | Бот путал имена и использовал «Валеричи» в личке

**Ситуация:** В личных переписках бот обращался к пользователю как «Господа Валеричи» — коллективное обращение, уместное только в группе. Также путал имена (Виктор → Сергей) из-за того что брал имя из `group_context.md` без привязки к `user_id`.

**Решение:** В `router.py` `telegram_reply()`:
1. Определять тип чата по наличию `=== Последние сообщения группы ===` в контексте
2. В группе — использовать коллективное обращение «Валеричи»
3. В личке — обращаться к пользователю по имени из `username` параметра
4. Убрать `random.choice` коллективных обращений для лички

**Результат:** Бот корректно обращается по имени в личке, использует «Валеричи» только в группе.

---

## [2026-04-13] | 💬 claude-session | Бот отвечал на китайском

**Ситуация:** LLM иногда переключался на китайский язык в ответах (вероятно, под влиянием контекста или случайного шума).

**Решение:** Добавить жёсткое правило в `persona` в `router.py`: «Отвечай ТОЛЬКО на русском языке. Никогда не используй другие языки, даже если в контексте встретился текст на другом языке.»

**Результат:** Бот всегда отвечает на русском.

---

## [2026-04-13] | 💬 claude-session | Шумные приветствия в группе

**Ситуация:** Бот здоровался (`👋 Привет`) когда группа уже давно общалась без него.

**Решение:** В системный промпт `chat`-режима добавлено: «НИКОГДА не здоровайся (никаких 'привет', '👋', 'добрый день') — пользователь уже в разговоре, отвечай сразу по делу.»

**Результат:** Бот отвечает по делу без лишних приветствий.

---

## [2026-04-13] | 💬 claude-session | Риски не учитывались в гипотезах

**Ситуация:** Sergey Sednev отметил: «спорно, практически ничего нового, риски не учтены». Генерация гипотез не требовала явного выделения рисков.

**Решение:** Усилить системные промпты для `hypothesize`/`brainstorm`/`rate` в `router.py`:
- `hypothesize`: «В НАЧАЛЕ ответа обязательно выдели секцию '⚠️ Риски'»
- `brainstorm`: «Укажи риски для каждой идеи»
- `rate`: «Выдели ключевые риски»

**Результат:** Каждый ответ включает явный анализ рисков.

---

## [2026-04-13] | 💬 claude-session | Code review fixes (7 fixes after adversarial review)

**Ситуация:** После code review выявлено 6 major + 9 minor проблем.

**Решение — 7 фиксов:**

1. **Voice дубль в личке** (Major) — убран лишний `telegram_reply`. Теперь: транскрипт + анализ, без третьего LLM-ответа.
2. **detect_unsupported после detect_intent** (Major) — составные запросы типа «запусти пайплайн и гипотезы» проходят по intent, unsupported только как fallback.
3. **summarize_voice_message → get_summary()** (Major) — убран хардкод `[:700]`, используется `context_builder.get_summary()`.
4. **_pending_intents TTL cleanup** (Major) — записи с `created_at`, автоочистка старше 5 мин при каждом callback.
5. **.reply → .answer после edit_reply_markup** (Major) — в proj и intent callback handlers.
6. **Импорты uuid/time на уровень модуля** (Minor) — убраны inline-импорты.
7. **asyncio.to_thread для файловых записей** (Minor) — `artifact.write_text` и `append_group_message` обёрнуты.

**Результат:** Все 7 фиксов применены, бот перезапущен.

---

## [2026-04-13] | 💬 claude-session | Бот не умеет запускать пайплайн по текстовой команде

**Ситуация:** Пользователь написал в личке «запусти пайплайн» — бот обработал это как обычный chat-режим, ответил разговором, но никакой stage не стартовал. `state.json` не создан, артефакты не генерировались.

**Решение:** Добавить `detect_unsupported()` — список ключевых фраз, которые бот НЕ может выполнить через обычный текст. При совпадении — бот честно признаёт ограничение и подсказывает правильную команду:
- «запусти пайплайн» → «Я пока не умею... Используй: `/continue`»
- «начни с нуля» → «Используй: `/new <имя>`»
- «проведи интервью» → «Это ручной этап...»
- и т.д.

Проверка вставлена в 3 места: приватный чат (основной), гостевой приватный, группа.

**Результат:** Бот честно признаёт что не умеет, а не молча генерит бесполезный ответ.

---

## [2026-04-13] | 💬 claude-session | Голосовые без постобработки

**Ситуация:** Виктор Степаненко запросил постобработку голосовых — просто транскрипции недостаточно.

**Решение:** Новая функция `summarize_voice_message()` в `router.py`. После транскрипции LLM делает:
1. 📝 Краткое саммари (2-3 предложения)
2. 🎯 Извлечённые действия/решения
3. 💡 Рекомендация по PMF-процессу

Обработчик голосовых в `telegram_bot.py` теперь после транскрипции автоматически запускает анализ и присылает результат.

**Результат:** Голосовые теперь дают структурированный анализ вместо сырого текста.

---

---

## [2026-04-10] | 💬 claude-session | Слэш-команды исчезли из личного формата после добавления меню

**Ситуация:** После внедрения persistent ReplyKeyboardMarkup Telegram перестал показывать список `/` команд в интерфейсе — `set_my_commands()` никогда не вызывался, бот не регистрировал свои команды.

**Решение:** Добавить `_register_commands()` в `main()` — вызывает `bot.set_my_commands()` со scope `BotCommandScopeAllPrivateChats`. Команды: `/hypothesize`, `/brainstorm`, `/rate`, `/projects`, `/use`, `/new`, `/status`, `/export`, `/balance`, `/feedback`.

**Результат:** `/` меню в личке показывает все команды с описанием.

---

## [2026-04-10] | 💬 claude-session | Дублирование гипотез при повторных запросах

**Ситуация:** При повторном `/hypothesize` LLM повторял старые гипотезы из `group_context.md` и добавлял новые сверху — пользователь получал нарастающий список.

**Решение:** Для режимов `hypothesize`/`brainstorm`/`rate` использовать только `project_summary.md` без истории чата. История чата нужна только для `chat`-режима.

**Результат:** Каждый запрос генерирует свежие независимые результаты.

---

## [2026-04-10] | 💬 claude-session | Система гостевого доступа (invite-токены)

**Ситуация:** Не было возможности дать отдельным участникам доступ к личному боту в рамках конкретного проекта.

**Решение:** Invite-токены. Владелец: `/share <проект>` → одноразовая ссылка (48ч). Гость: кликает → регистрируется → полный личный чат в рамках проекта. `/revoke <user_id>` — отзыв. Все действия гостей логируются в `data/guest_activity.json`. Раздать доступ можно через: команду в группе, панель WebUI (таблица участников с кнопкой «Пригласить»).

**Результат:** Контролируемый гостевой доступ с мониторингом активности.

---

## [2026-04-10] | 💬 claude-session | Мониторинг баланса OpenRouter в WebUI

**Ситуация:** Баланс OpenRouter был виден только через Telegram-команду `/balance` или в логах.

**Решение:** Виджет в дашборде WebUI — показывает остаток, usage/limit, при остатке < $2 меняет цвет на красный. Эндпоинт `/api/balance`.

**Результат:** Баланс виден в панели без обращения к боту.

---

## [2026-04-10] | 💬 claude-session | Бот не отвечает в группе

**Ситуация:** После преобразования группы в супергруппу бот перестал отвечать — ID сменился с `-5202174865` на `-1003611295657` (формат `-100XXXXXXXXX`), но `group_links.json` хранил старый.

**Решение:** Обновить `group_links.json` с правильным ID. При привязке группы всегда сохранять актуальный `message.chat.id` от aiogram.

**Результат:** Бот отвечает в группе.

---

## [2026-04-10] | 💬 claude-session | TelegramConflictError — два экземпляра бота

**Ситуация:** Бот упал с `TelegramConflictError` — параллельно работали systemd-сервис и ручной `nohup`-запуск.

**Решение:** Убить оба процесса, перезапустить только через `sudo systemctl restart marketbot`. Добавить watchdog: PID-файл + heartbeat раз в 30 минут. На старте: если heartbeat свежий — убить себя (мы новее); если протухший — убить старый процесс и стартовать нормально.

**Результат:** Гарантируется один экземпляр бота.

---

## [2026-04-10] | 💬 claude-session | Бот путал имена пользователей (Виктор → Сергей)

**Ситуация:** LLM обращался к Виктору как «Сергей Валерич» — брал имя из истории `group_context.md` без привязки к текущему `user_id`.

**Решение:** Создать `projects/<проект>/users.json` с маппингом `user_id → имя`. Передавать текущего пользователя явно в системный промпт через параметр `username`.

**Результат:** Бот корректно обращается по имени к каждому пользователю.

---

## [2026-04-10] | 💬 claude-session | Шумные уведомления «📝 Записано»

**Ситуация:** Бот присылал «📝 Записано» на каждое обычное сообщение в группе — замусоривало чат.

**Решение:** Убрать уведомление для рядовых сообщений. Оставить только для значимых действий (привязка проекта, явные команды).

**Результат:** Чат не загрязняется служебными уведомлениями.

---

## [2026-04-10] | 💬 claude-session | Бот каждый раз спрашивал проект в личке

**Ситуация:** При каждом запуске `active_project` был пустым — хранился только в памяти.

**Решение:** Сохранять выбранный проект в `data/user_sessions.json`. При первом обращении пользователя восстанавливать из файла.

**Результат:** Бот помнит проект между перезапусками.

---

## [2026-04-10] | 💬 claude-session | Постоянное меню проекта в личке

**Ситуация:** Не было удобного способа видеть текущий проект и переключаться — нужно было каждый раз явно выбирать.

**Решение:** Добавить `ReplyKeyboardMarkup` с кнопками текущего проекта, «🔄 Сменить проект» и «➕ Новый проект» — закреплено внизу как постоянное меню.

**Результат:** Пользователь всегда видит контекст и может переключиться одним тапом.

---

## [2026-04-10] | 💬 claude-session | Pydantic «Instance is frozen» — два случая

**Ситуация:** Бот вылетал с `ValidationError: Instance is frozen` в двух местах:
1. В обработчике голосовых: `message.text = text`
2. В proj-callback: `fake.from_user = callback.from_user`

aiogram 3 использует frozen Pydantic-модели — мутировать поля нельзя.

**Решение:** Инлайнить логику напрямую без мутации объекта. Не копировать поля модели вручную.

**Результат:** Голосовые и смена проекта работают без крашей.

---

## [2026-04-10] | 💬 claude-session | Детекция интентов с предложением команд

**Ситуация:** Пользователь пишет что-то похожее на запрос гипотез/брейншторма/оценки — бот отвечал в обычном chat-режиме, не предлагая специализированную команду.

**Решение:** Добавить `detect_intent(text)` — сравнивает сообщение с ключевыми словами `_INTENTS`. Если совпадение — показывает инлайн-кнопку с подходящим режимом (`/hypothesize`, `/brainstorm`, `/rate`).

**Результат:** Бот сам подсказывает уместную функцию.

---

## [2026-04-10] | 💬 claude-session | Голосовые в личке через пересылку

**Ситуация:** Пересланные голосовые не обрабатывались — бот не транскрибировал чужие войсы.

**Решение:** Обработчик голосовых в личке теперь принимает любое voice-сообщение независимо от источника, транскрибирует через Whisper и отвечает в контексте активного проекта.

**Результат:** Голосовые работают в личке.

---

## [2026-04-10] | 💬 claude-session | WebUI: объединить обработку контекста и индексацию

**Ситуация:** Две отдельные кнопки — «Обработать контекст» и «Индексировать» — пользователь должен был нажимать обе последовательно.

**Решение:** Добавить `/api/ctx_and_index` — один эндпоинт, сначала обрабатывает контекст, затем индексирует. Одна кнопка «Обработать и индексировать».

**Результат:** Один клик вместо двух, порядок гарантирован.

---

## [2026-04-10] | 💬 claude-session | Race condition: semaphore в callback-хэндлерах

**Ситуация:** (Codex adversarial review, HIGH) `handle_proj_callback` и `handle_intent_callback` мутировали `active_project` вне per-user семафора. При быстрой смене проекта + отправке сообщения бот мог ответить из контекста старого проекта.

**Решение:** Обернуть оба callback-хэндлера в `async with get_semaphore(callback.from_user.id)`.

**Результат:** Нет race condition при переключении проекта.

---

## [2026-04-10] | 💬 claude-session | Race condition: запись артефактов

**Ситуация:** (Codex adversarial review, HIGH) Для `hypothesize`/`brainstorm`/`rate` использовался read-modify-write в один файл без блокировки. Два одновременных callback могли затереть друг друга.

**Решение:** Каждый ответ сохраняется в отдельный timestamped файл (`YYYY-MM-DD_HH-MM-SS.md`) в поддиректорию (`hypotheses/`, `brainstorm/`, `ratings/`). Атомарно, без конкурентного доступа.

**Результат:** Артефакты не теряются при параллельных запросах.

---

## [2026-04-10] | 💬 claude-session | Watchdog — мониторинг экземпляров бота

**Ситуация:** Не было автоматической защиты от двух запущенных экземпляров — приходилось вручную искать и убивать процессы.

**Решение:** При старте: читать `data/bot.pid` + `data/bot.heartbeat`. Heartbeat свежий (<30 мин) → `sys.exit(0)` (убиваем себя, мы новее). Heartbeat протухший → `SIGTERM` старому PID, стартуем нормально. Heartbeat обновляется каждые 30 минут в фоне.

**Результат:** Автоматическое разрешение конфликта экземпляров.

---

## [2026-04-10] | 💬 claude-session | Единая система фидбека (4 источника)

**Ситуация:** Не было единого места для сбора замечаний по механике бота. `/feedback` работал только в личке, без структуры.

**Решение:** Единый файл `data/bot_feedback.md`. Источники:
- 📱 личка — команда `/feedback`
- 👥 группа — та же команда в группе
- 🌐 webui — форма «Обратная связь» в дашборде
- 💬 claude-session — записи из сессий работы над ботом (этот файл)

Каждая запись: Ситуация → Решение → Результат.

**Результат:** Все замечания и решения в одном месте.

---
